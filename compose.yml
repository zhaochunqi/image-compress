services:
  image-compress:
    image: zhaochunqi/image-compress
    environment:
      - LOSSLESS=true
      - CONVERT_TO_WEBP=true
    volumes:
      - ./source:/app/source
      - ./compressed:/app/compressed
