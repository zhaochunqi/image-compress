services:
  image-compressor:
    image: ghcr.io/zhaochunqi/image_compress:latest
    environment:
      - LOSSLESS=true
      - CONVERT_TO_WEBP=true
    volumes:
      - ./source:/app/source
      - ./compressed:/app/compressed
