services:
  image-compress:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - LOSSLESS=true
      - CONVERT_TO_WEBP=true
    volumes:
      - ./source:/app/source
      - ./compressed:/app/compressed
